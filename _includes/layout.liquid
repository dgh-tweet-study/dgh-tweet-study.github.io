<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <!-- <link rel="stylesheet" type="text/css" href="{{ '/css/styles.css' | url }}"> -->
    <link rel="stylesheet" type="text/css" href="css/styles.css">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
  </head>
    <body class="px-2">
        {% include "header.liquid" %}
    <main class="page-content" aria-label="Content">
      <div class="mt-8 mx-auto prose prose-h2:text-center prose-h2:font-semibold prose-em:text-xs prose-a:text-decoration-line prose-a:decoration-orange-500 prose-a:text-gray-700 prose-a:font-normal prose-a:underline-offset-4 prose-ul:list-circle prose-ol:list-decimal">
        {{ content }}
      </div>
    </main>
    {% include "footer.liquid" %}
  </body>

</html>